
<html lang = "en">
  <head>
    <meta charset = "utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
   <link rel='stylesheet' href='/stylesheets/slider.css' />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
     <style>
      .container{
        position: absolute;
        top: 50%;
        left: 50%;
        -moz-transform: translateX(-50%) translateY(-50%);
        -webkit-transform: translateX(-50%) translateY(-50%);
        transform: translateX(-50%) translateY(-50%);
      }
    </style> 
  </head>
  <div class="jumbotron text-center">
    <h3>GL Electrics</h3>
    <p>Change Password</p>
  </div>
  <div class="container p-3 my-3 bg-dark text-white" >
      <form class="form-horizontal" role="form" action="/updatecredentials" method="post" onSubmit="return checkblank(this);">
      <% if (message.length > 0) { %>
          <div class="alert alert-success col-sm-12"><%= message %></div>
    <% } %>
    <div id="signupalert" style="display:none" class="alert alert-danger">
      <p>Error:</p>
      <span></span>
    </div>
    <div class="form-group">
        <label for="oldpwd">Old Password:</label>
        <input class="form-control" type="password" name="oldpass" id="oldpass">
    </div>
    <div class="form-group">
        <label for="newpwd">New Password:</label>
        <input class="form-control" type="password" name="newpass" id="newpass">
    </div>
            <!-- Button -->   
    <div class="form-group">
        <div class="col-md-offset-3 col-md-9">
            <button id="btn-updatecred" type="submit" class="btn btn-info"><i class="icon-hand-right"></i>&nbsp Change password</button>
        </div>
    </div>
      </form>
  </div>
</html>